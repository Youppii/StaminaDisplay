package me.noobilybridge.config;

public enum NumberPosition {
        BEFORE_NAME,
        AFTER_NAME,
        BAR_CENTER,
        FOLLOW_BAR,
        OVER_NAME,
        BAR_RIGHT,
        BAR_LEFT,
        BEFORE_BAR,
        AFTER_BAR,
    }
